---
allOf:
- $ref: "./NewRecurringUnavailability.yaml"
- type: object
  properties:
    id:
      $ref: "./UnavailabilityId.yaml"
  required:
    - id
description: >
  A recurring unavailability. This could be a maintenance window, a booking, or something else.
  The server will send this to clients