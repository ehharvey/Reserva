---
allOf:
  - $ref: "./NewUser.yaml"
  - type: object
    properties:
      role:
        type: string
        enum: [standard]
        default: standard
        description: The user's role, which is automatically set to "standard".