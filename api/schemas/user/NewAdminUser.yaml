---
allOf:
  - $ref: "./NewUser.yaml"
  - type: object
    properties:
      role:
        type: string
        enum: [admin]
        default: admin
        description: The user's role, which is automatically set to "admin".