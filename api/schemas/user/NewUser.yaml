---
allOf:
- $ref: './UpdateUser.yaml'
- type: object
  required:
    - username
    - email
    - password
    - role
  properties:
    role:
      type: string
      enum: [standard, admin]
      default: user
      description: The user's role.