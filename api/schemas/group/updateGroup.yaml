---
type: object
properties:
  name:
    type: string
    example: project group
    description: the name of the group.
description: >
  a group. this could be a project group, a team, or something else.
  this is sent by the client when updating an existing group.